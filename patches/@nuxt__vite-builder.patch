diff --git a/dist/index.mjs b/dist/index.mjs
index 708834b11aa5fbf20d80dad8f632ffd27d0a5b06..bde13a0fa75cdf7bc5dd9e48314e8cedc7e12a38 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -11,6 +11,7 @@ import vuePlugin from "@vitejs/plugin-vue";
 import { getQuery, joinURL, withLeadingSlash, withTrailingSlash, withoutBase, withoutLeadingSlash } from "ufo";
 import { filename } from "pathe/utils";
 import { resolveModulePath } from "exsolve";
+import { onigiriCompilerPlugin, onigiriManifestPlugin } from "vue-onigiri";
 import process from "node:process";
 import "unplugin";
 import escapeStringRegexp from "escape-string-regexp";
@@ -293,11 +294,15 @@ function ViteNodePlugin(nuxt) {
 	const serverEntryCode = `export { default } from ${JSON.stringify(pathToFileURL(serverResolvedPath).href)}`;
 	setBuildOutput("serverEntry", () => serverEntryCode);
 	setBuildOutput("clientManifest", () => `import { viteNodeFetch } from ${JSON.stringify(pathToFileURL(fetchResolvedPath))};export default () => viteNodeFetch.getManifest()`);
+	const islandsTemplatePath = join(nuxt.options.buildDir, "components.islands.mjs");
+	const islandsCode = `import runner from ${JSON.stringify(pathToFileURL(runnerResolvedPath).href)};const m = await runner.executeFile(${JSON.stringify(islandsTemplatePath)});export const islandComponents = m.islandComponents`;
 	nitro.options.virtual ||= {};
 	nitro.options._config.virtual ||= {};
 	const runnerCode = `export { default } from ${JSON.stringify(pathToFileURL(runnerResolvedPath).href)}`;
 	nitro.options.virtual["#build/dist/server/runner.mjs"] = runnerCode;
 	nitro.options._config.virtual["#build/dist/server/runner.mjs"] = runnerCode;
+	nitro.options.virtual["#build/dist/server/components.islands.mjs"] = islandsCode;
+	nitro.options._config.virtual["#build/dist/server/components.islands.mjs"] = islandsCode;
 	let legacySsrServer;
 	return {
 		name: "nuxt:vite-node-server",
@@ -1247,6 +1252,7 @@ const clientEnvironment = (nuxt, entry) => {
 				"#head",
 				"virtual:nuxt:",
 				"virtual:nuxt:*",
+				"vue-onigiri",
 				...getTranspileStrings({
 					isDev: nuxt.options.dev,
 					isClient: true
@@ -1466,7 +1472,8 @@ function ssr(nuxt) {
 			"/__vue-jsx",
 			"#app",
 			/^nuxt(\/|$)/,
-			/(nuxt|nuxt3|nuxt-nightly)\/(dist|src|app)/
+			/(nuxt|nuxt3|nuxt-nightly)\/(dist|src|app)/,
+			"vue-onigiri"
 		]
 	};
 }
@@ -1478,7 +1485,10 @@ function ssrEnvironment(nuxt, serverEntry) {
 			outDir: resolve(nuxt.options.buildDir, "dist/server"),
 			ssr: true,
 			rolldownOptions: {
-				input: { server: serverEntry },
+				input: {
+					"server": serverEntry,
+					"components.islands": resolve(nuxt.options.buildDir, "components.islands")
+				},
 				external: [
 					"nitro/runtime",
 					"#internal/nitro",
@@ -2578,6 +2588,192 @@ const bundle = async (nuxt) => {
 		config.build.watch = void 0;
 	}
 	userOptimizeDepsInclude.set(nuxt, [...config.optimizeDeps?.include || []]);
+	const vscPlugin = {
+		name: "nuxt:virtual-vsc",
+		resolveId: {
+			order: "pre",
+			handler(id) {
+				if (id.startsWith("virtual:vsc:")) return id.slice(12);
+				if (id.startsWith("/@id/virtual:vsc:")) return id.slice(17);
+			}
+		}
+	};
+	const onigiriComponentImports = /* @__PURE__ */ new Map();
+	const ingestComponents = (components) => {
+		for (const c of components) {
+			if (!c.pascalName || !c.filePath) continue;
+			if (c._raw) continue;
+			if (c.mode === "client") continue;
+			onigiriComponentImports.set(c.pascalName, {
+				path: c.filePath.replaceAll("\\", "/"),
+				export: c.export
+			});
+		}
+	};
+	for (const app of Object.values(nuxt.apps)) if (app.components) ingestComponents(app.components);
+	nuxt.hook("components:extend", (components) => {
+		onigiriComponentImports.clear();
+		ingestComponents(components);
+	});
+	const onigiriChunkMap = /* @__PURE__ */ new Map();
+	const rootDir = nuxt.options.rootDir.replaceAll("\\", "/").replace(/\/$/, "");
+	const toRootRelative = (absPath) => {
+		const n = absPath.replaceAll("\\", "/");
+		return n.startsWith(rootDir + "/") ? n.slice(rootDir.length) : n;
+	};
+	const resolveChunkUrl = (sourcePath) => {
+		if (nuxt.options.dev) return joinURL(nuxt.options.app.baseURL, nuxt.options.app.buildAssetsDir, sourcePath);
+		if (!sourcePath.startsWith("/") && !sourcePath.startsWith(".") && !/^[a-z]:[\\/]/i.test(sourcePath)) return joinURL(nuxt.options.app.baseURL, nuxt.options.app.buildAssetsDir, sourcePath);
+		return onigiriChunkMap.get(sourcePath);
+	};
+	const onigiriCompilerOptions = {
+		additionalImports: () => onigiriComponentImports,
+		resolveChunkUrl
+	};
+	const onigiriPlugins = [
+		vscPlugin,
+		onigiriCompilerPlugin(onigiriCompilerOptions),
+		onigiriManifestPlugin(),
+		{
+			name: "nuxt:onigiri-chunk-collector",
+			applyToEnvironment: (env) => env.name === "client",
+			generateBundle(outputOptions, bundle) {
+				if (String(outputOptions?.dir || "").replaceAll("\\", "/").includes("/dist/server")) return;
+				onigiriChunkMap.clear();
+				for (const [fileName, chunk] of Object.entries(bundle)) {
+					if (chunk.type !== "chunk") continue;
+					const publicUrl = joinURL(nuxt.options.app.baseURL, nuxt.options.app.buildAssetsDir, fileName);
+					for (const moduleId of chunk.moduleIds || []) {
+						if (!/\.(?:vue|m?[jt]sx?)(?:\?.*)?$/.test(moduleId)) continue;
+						if (moduleId.includes("node_modules")) continue;
+						onigiriChunkMap.set(toRootRelative(moduleId), publicUrl);
+					}
+				}
+			}
+		}
+	];
+	config.plugins.push(...onigiriPlugins);
+	const buildClientImportFnSource = () => {
+		const prefix = joinURL(nuxt.options.app.baseURL, nuxt.options.app.buildAssetsDir);
+		const entries = [];
+		const seen = /* @__PURE__ */ new Set();
+		for (const [, entry] of onigiriComponentImports) {
+			if (!entry?.path) continue;
+			if (entry.path.startsWith("/") || entry.path.startsWith(".") || /^[a-z]:[\\/]/i.test(entry.path)) continue;
+			if (seen.has(entry.path)) continue;
+			seen.add(entry.path);
+			const url = joinURL(prefix, entry.path);
+			entries.push(`  ${JSON.stringify(url)}: () => import(${JSON.stringify(entry.path)})`);
+		}
+		return `
+const __chunkMap = {
+${entries.join(",\n")}
+}
+export const manifest = {}
+export async function importFn(src, exportName = 'default') {
+  const loader = __chunkMap[src]
+  if (loader) {
+    const mod = await loader()
+    return mod[exportName] ?? mod.default ?? mod
+  }
+  if (src.startsWith('/')) {
+    const mod = await import(/* @vite-ignore */ src)
+    return mod[exportName] ?? mod.default ?? mod
+  }
+  throw new Error('[vue-onigiri] No loader registered for chunk "' + src + '"')
+}
+`;
+	};
+	const buildServerImportFnSource = () => {
+		const prefix = joinURL(nuxt.options.app.baseURL, nuxt.options.app.buildAssetsDir);
+		const reverseMap = {};
+		for (const [src, url] of onigiriChunkMap) reverseMap[url] = src;
+		return `
+export const manifest = {}
+const PREFIX = ${JSON.stringify(prefix)}
+const REVERSE = ${JSON.stringify(reverseMap)}
+export async function importFn(src, exportName = 'default') {
+  let spec = REVERSE[src] || (src.startsWith(PREFIX) ? src.slice(PREFIX.length) : src)
+  if (spec.startsWith('/')) spec = spec.slice(1)
+  const mod = await import(/* @vite-ignore */ spec)
+  return mod[exportName] ?? mod.default ?? mod
+}
+`;
+	};
+	config.plugins.push({
+		name: "nuxt:onigiri-client-manifest",
+		enforce: "pre",
+		applyToEnvironment: (env) => env.name === "client",
+		resolveId: {
+			order: "pre",
+			handler(id) {
+				if (id === "virtual:onigiri/manifest") return "\0virtual:onigiri/manifest";
+			}
+		},
+		load(id) {
+			if (id !== "\0virtual:onigiri/manifest") return;
+			return buildClientImportFnSource();
+		}
+	});
+	config.plugins.push({
+		name: "nuxt:onigiri-server-manifest",
+		enforce: "pre",
+		applyToEnvironment: (env) => env.name !== "client",
+		resolveId: {
+			order: "pre",
+			handler(id) {
+				if (id === "virtual:onigiri/manifest") return "\0virtual:onigiri/manifest";
+			}
+		},
+		load(id) {
+			if (id !== "\0virtual:onigiri/manifest") return;
+			return buildServerImportFnSource();
+		}
+	});
+	const onigiriNitroManifestOverride = {
+		name: "nuxt:onigiri-nitro-manifest",
+		enforce: "pre",
+		resolveId: {
+			order: "pre",
+			handler(id) {
+				if (id === "virtual:onigiri/manifest") return "\0virtual:onigiri/manifest";
+			}
+		},
+		load(id) {
+			if (id !== "\0virtual:onigiri/manifest") return;
+			return buildServerImportFnSource();
+		}
+	};
+	const onigiriNitroPlugins = () => [
+		{
+			name: "nuxt:virtual-vsc",
+			resolveId: {
+				order: "pre",
+				handler(id) {
+					if (id.startsWith("virtual:vsc:")) return id.slice(12);
+				}
+			}
+		},
+		onigiriNitroManifestOverride,
+		onigiriCompilerPlugin(onigiriCompilerOptions),
+		onigiriManifestPlugin({ stub: true })
+	];
+	useNitro().hooks.hook("rollup:before", (_, rollupConfig) => {
+		rollupConfig.plugins.unshift(...onigiriNitroPlugins());
+	});
+	useNitro().hooks.hook("prerender:config", (preConfig) => {
+		preConfig.rollupConfig ||= {};
+		preConfig.rollupConfig.plugins ||= [];
+		preConfig.rollupConfig.plugins.unshift(...onigiriNitroPlugins());
+	});
+	const updateReverseMap = () => {
+		const reverse = {};
+		for (const [src, url] of onigiriChunkMap) reverse[url] = src;
+		nuxt.options.nitro.virtual ||= {};
+		nuxt.options.nitro.virtual["#internal/nuxt/onigiri-reverse-map"] = () => `export default ${JSON.stringify(reverse)}`;
+	};
+	nuxt.hook("build:done", updateReverseMap);
+	nuxt.hook("vite:compiled", updateReverseMap);
 	const ctx = {
 		nuxt,
 		entry,
diff --git a/dist/vite-node-entry.d.mts b/dist/vite-node-entry.d.mts
index 8060d48603010d79f23e2b640e33f7d8584820e6..5610ad02da537338c8f6e26a05350b0f069c1a3b 100644
--- a/dist/vite-node-entry.d.mts
+++ b/dist/vite-node-entry.d.mts
@@ -1,6 +1,6 @@
-import { NuxtSSRContext } from "nuxt/app";
+import { EntryOptions, NuxtSSRContext } from "nuxt/app";
 
 //#region src/vite-node-entry.d.ts
-declare const _default: (ssrContext: NuxtSSRContext) => Promise<any>;
+declare const _default: (ssrContext: NuxtSSRContext, options?: EntryOptions) => Promise<any>;
 //#endregion
 export { _default as default };
\ No newline at end of file
diff --git a/dist/vite-node-entry.mjs b/dist/vite-node-entry.mjs
index c6982639ea7690e981d78f39935a0c5dd2d64108..1776953f90817269cdd8ed281c54a2d05c656e35 100644
--- a/dist/vite-node-entry.mjs
+++ b/dist/vite-node-entry.mjs
@@ -5,7 +5,7 @@ import { viteNodeFetch, viteNodeOptions } from "#vite-node";
 import runner from "#vite-node-runner";
 //#region src/vite-node-entry.ts
 let render;
-var vite_node_entry_default = async (ssrContext) => {
+var vite_node_entry_default = async (ssrContext, options) => {
 	process.server = true;
 	import.meta.server = true;
 	const invalidates = await viteNodeFetch.getInvalidates();
@@ -16,7 +16,7 @@ var vite_node_entry_default = async (ssrContext) => {
 		const time = Math.round((performance.now() - start) * 1e3) / 1e3;
 		consola.success(`Vite server hmr ${updates.size} files`, time ? `in ${time}ms` : "");
 	}
-	return await render(ssrContext);
+	return await render(ssrContext, options);
 };
 //#endregion
 export { vite_node_entry_default as default };
